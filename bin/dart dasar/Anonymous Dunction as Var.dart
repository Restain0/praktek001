var upperFunction = (String name) {
  return name.toUpperCase();
};
var lowerFunction = (String name) {
  return name.toLowerCase();
};

void main() {
  print(upperFunction("Akbar"));
  print(lowerFunction("RAMADHAN"));
}
